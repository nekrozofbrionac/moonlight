import { EnvironmentDefinition } from "./environment-definition";
import { hidden } from "./environment-hidden";

export const environment: EnvironmentDefinition = {
  production: false,
  pocketBaseLink: hidden.pbLink,
}
