import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'de.ariunbat.moonlight',
  appName: 'Moonlight',
  webDir: 'dist/moonlight',
  bundledWebRuntime: false
};

export default config;
